# Your GitHub Personal Access Token (PAT)
# Needs `repo` and `project` scopes.
GITHUB_TOKEN=your_token_here

# We will fill these in once we query the GraphQL API
GITHUB_PROJECT_ID=
GITHUB_STATUS_FIELD_ID=